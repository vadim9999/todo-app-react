import {styled} from 'styled-components'

// export const editableCell= () => styled.
//     position:"relative";


export const editableCellWrap = {
    padding: "5px 12px",
    cursor: "pointer",
}

export let editableRrow, editableCellValueWrap = {
    border: '1px solid #d9d9d9',
    borderRadius: '4px',
    padding: '4px 11px'
}